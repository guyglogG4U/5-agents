# .env 🔐

## מה הוא עושה
קובץ משתני סביבה רגישים — **לא** נכלל ב-git (ב-`.gitignore`).

מכיל בדרך כלל:
- `OPENAI_API_KEY` — נדרש ל-[[../Agents/יובל — מעצב התמונות]] (OpenAI Images API)
- מפתחות API נוספים לפי צורך

## קובץ דוגמה
`.env.example` — גרסה ריקה שכן נכנסת ל-git, מדגימה אילו משתנים נדרשים.

## מיקום
`/.env` — שורש הפרויקט

## למי משויך
- [[../Agents/יובל — מעצב התמונות]] — משתמש ב-`OPENAI_API_KEY`
- [[../Skills/gpt-image-gen]] — קורא את המפתח בזמן ריצה

## קבצים קשורים
- `.env.example`
- [[.gitignore]]
